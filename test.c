#include <stdio.h>
#include <stdlib.h>
// #define defif 1
int main()
{
#ifdef defif
    printf("MMuuaaahhhh");
#endif
    // int n;
    // scanf("%d", &n);
    // int *arr = (int *)malloc(n * sizeof(int));
    return 0;
}